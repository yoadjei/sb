import 'package:flutter/material.dart';

class StadiumColors {
  // Dark stadium field
  static const navy = Color(0xFF0B1F33);
  static const navyMid = Color(0xFF132F4C);

  // Brand mark / ArenaBoard logo (warm LED amber)
  static const brand = Color(0xFFFFB020);

  // Action / Team A
  static const accent = Color(0xFF2EE59A);

  // Team B / destructive
  static const rival = Color(0xFFFF6B4A);

  // Light surfaces
  static const surfaceLight = Color(0xFFF4F7FB);
  static const surfaceLightMid = Color(0xFFE8EEF6);
  static const cardLight = Color(0xFFFFFFFF);
  static const textDark = Color(0xFF14213D);
  static const textMutedLight = Color(0xFF5C6B7A);
}
